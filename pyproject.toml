[project]
name = "litellm-proxy-config"
version = "0.1.0"
description = "Dynamic configuration updater for LiteLLM Proxy"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false
